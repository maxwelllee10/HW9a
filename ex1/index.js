const circle = require('./circle');

const radius = 5;

console.log(`Circumference of a circle with radius ${radius} is ${circle.circumference(radius)}`);
console.log(`Area of a circle with radius ${radius} is ${circle.area(radius)}`);
